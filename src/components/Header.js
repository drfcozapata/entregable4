import React from 'react';
import header from '../img/banner.jpg';

const Header = () => {
	return (
		<div className="header">
			<img src={header} alt="Banner usuarios" />
		</div>
	);
};

export default Header;
